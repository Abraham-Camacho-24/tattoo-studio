import Image from "next/image";
import { portfolioPieces } from "@/data/portfolio";
export function Portfolio() { return <section className="section portfolio" id="work"><div className="shell"><div className="portfolio-intro reveal"><div className="section-kicker"><span>03</span> PORTAFOLIO</div><h2>SELECTED <em>WORK</em></h2><p>Una selección de piezas. Sustituye las imágenes de demostración por fotografías de la obra real en <code>data/portfolio.ts</code>.</p></div><div className="portfolio-grid">{portfolioPieces.map((piece, index) => <figure className={`piece piece-${piece.aspect} reveal`} key={piece.title} style={{ animationDelay: `${index * 60}ms` }}><Image src={piece.image} alt={piece.alt} fill sizes="(max-width: 700px) 100vw, (max-width: 1024px) 50vw, 33vw" /><figcaption><span>{piece.type}</span><strong>{piece.title}</strong></figcaption></figure>)}</div></div></section>; }
